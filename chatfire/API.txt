Socrates
API-Key = sk-vbxHO8B2I8JaXhPG5SakQ4PT5gLisEvuMyqIr1ouTIAYppX0
API 地址 = https://api.chatfire.ai
model: deepseek-v3-2-251201

下一步不要再扩文档了，应该开始把 [develop.md](/Users/kyrie/Downloads/OpenHands-1.5.0/MultiAgent_FPGA/develop.md) 里的 AES MVP 变成“第一条能跑通的闭环”。顺序我建议固定成这样：

1. 先落 `SpecIR` 和 `PlanDAG` 的静态样例。
   目标是把 AES-128 / encrypt-only / block-handshake / iterative-10-round 这组决策固化成一个最小 JSON 契约，避免后面 RTL 和 testbench 再反复改口。

2. 先做顶层 `aes128_encrypt_core`，不要一开始四个模块全铺开。
   你当前最重要的是打通 `L0 -> L1 -> integration regression` 这条链。先有一个能过 NIST KAT、能输出 `CHECKPOINT|...` 的顶层核，比先把 `aes_sbox`、`key_schedule`、`round_transform` 全部拆漂亮更重要。

3. 紧接着补 `.cpp` testbench 规范和向量文件规范。
   这是你当前 Verilator-MCP 路径的硬前提。没有稳定 `.cpp` TB、`simulation.log` checkpoint 约定、`+vecfile` 约定，后面的 L1/L2 都只是空设计。

4. 再拆子模块并回填 `PlanDAG`。
   当顶层核跑通后，再把 `aes_sbox`、`aes_key_schedule_128`、`aes_round_transform` 抽出来，做模块级 L0/L1，最后让 `aes128_encrypt_core` 变成真正的 integration top。

5. 最后才做 orchestrator 代码。
   先有可运行的 artifacts、TB、Verilator 命令契约，再写状态机；不要反过来。

你现在最值得做的“下一个具体任务”其实是这三个交付物：

- `SpecIR.json` 样例
- `PlanDAG.json` 样例
- `tb/aes128_encrypt_core_tb.cpp` 的固定 checkpoint / plusargs / vecfile 约定

**如何充分利用 Codex App**

把 Codex App 当成“主控 + 并行研究员 + 执行器”，不要当成聊天窗口。

- 用主线程做主控。
  主线程只负责定范围、锁接口、做最终集成决策。像你现在这样让它维护 [develop.md](/Users/kyrie/Downloads/OpenHands-1.5.0/MultiAgent_FPGA/develop.md) 和 skill 体系，是对的。

- 只把独立问题交给 subagent。
  适合并行下发的任务：
  - `帮我设计 aes_sbox_tb.cpp 的 checkpoint 规则`
  - `帮我比较 iterative AES 和 unrolled AES 的 testbench 成本`
  - `帮我检查 Verilator-MCP 的 plusargs 最佳用法`
  不适合下发的任务：
  - `帮我决定整个架构怎么做`
  - `帮我完成当前主线 blocker`
  主线 blocker 还是你和主线程自己解。

- 把 skill 当“硬约束”，不是参考资料。
  现在你已经有：
  - [verilator_mcp_setup.md](/Users/kyrie/Downloads/OpenHands-1.5.0/.openhands/skills/verilator_mcp_setup.md)
  - [verilog_verilator.md](/Users/kyrie/Downloads/OpenHands-1.5.0/.openhands/skills/verilog_verilator.md)
  - [aes_verilator_profile.md](/Users/kyrie/Downloads/OpenHands-1.5.0/.openhands/skills/aes_verilator_profile.md)
  后面每次让 Codex 实现或分析 AES，都明确点名它们，这样模型不会自己漂移到 `.sv` testbench、auto-generated TB、natural language tool 这些你已经排除的路径上。

- 先让 Codex 产出“可执行中间产物”，不要让它直接大包大揽。
  最有效的请求格式是：
  - “先生成 `SpecIR.json` 样例”
  - “再生成 `PlanDAG.json` 样例”
  - “再实现 `aes128_encrypt_core_tb.cpp`”
  - “最后把这三者串成一次 Verilator-MCP 调用链”
  这比一句“帮我把 AES 毕设做完”有效得多。

- 用 Codex 做“强制验证”而不是只做生成。
  每次改完后直接让它：
  - 跑 `pre-commit`
  - 跑 Verilator compile/simulate
  - 检查 `simulation.log`
  - 总结 checkpoint 是否齐全
  这正好符合你现在“toolchain is the judge”的思路。

- 用 subagent 做 thesis 侧辅助，而不是抢主实现。
  很适合让 subagent 单独产出：
  - 论文里的架构图说明
  - AES 案例的实验设计
  - RISC-V 为什么作为 future work 的论证
  - 表格化对比项

**我对你下一步的直接建议**

下一轮就做这个，不要再发散：

1. 让我先帮你生成 AES MVP 的 `SpecIR.json` 样例
2. 再生成 `PlanDAG.json` 样例
3. 然后生成 `aes128_encrypt_core` 的 `.cpp` testbench 规范
4. 最后再开始写 RTL

如果你愿意，我下一步就可以直接从第 1 步开始，先把 AES 的 `SpecIR.json` 和 `PlanDAG.json` 样例定出来。
